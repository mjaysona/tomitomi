---
import { TagItem } from '@/interfaces/tag';

export interface Props extends TagItem{
  tag: string,
};

const { tag, size }:Props = Astro.props;
---
<a
  href={`/articles/tags/${tag.toLowerCase()}/`}
  class={`tag ${size}`}
>
  {tag}
</a>
<style lang="scss">
  @import '../styles/variables.scss';

  a.tag {
    color: $secondary-color;
    &.sm {
      font-size: 0.75rem;
    }
    &.md {
      font-size: 1rem;
    }
  }
</style>
